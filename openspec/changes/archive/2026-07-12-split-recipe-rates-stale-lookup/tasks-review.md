<!-- Append-only. Each review pass adds a new ## Pass N section. Never delete or edit previous passes. -->

## Pass 1 — 2026-07-12

**Source: Reviewer**

**Status: CONCERNS**

### Resolved from Previous Pass
(first pass: leave empty)

### Findings
[Task 2.2/2.3 — import instruction split across two tasks, ambiguous merge] — Confirmed `app/models/factory.ts:22-25` already has an existing `import { acceptAllSuggestions as acceptAllSuggestionsWalk, rejectAllSuggestions as rejectAllSuggestionsWalk } from "./suggestions";` block. Neither task states that the new named imports (`applyRejectSilent`, `lineRecipeSlugs` in 2.2; `applyRejectChoice` in 2.3) must be added to this *existing* import statement rather than as new, separate `import ... from "./suggestions"` statements (which would be a duplicate-source-import lint violation Biome would flag). Task 2.2's phrasing ("alongside the existing applyRejectChoice... import to be added in 2.3") reads as if `applyRejectChoice` already exists as an import today, which it doesn't (not in the current factory.ts import list) — it's being added by 2.3, and the phrasing doesn't make clear whether 2.2's edit should reserve space in the same import statement for 2.3 to extend, or whether 2.3 re-touches the same line 2.2 just wrote. Consolidate into a single task (or a single explicit sub-step) that adds all three names to the existing block in one edit.
[R5.S9/R5.S10 ordering clause — no task verifies it] — Spec's R5.S9 and R5.S10 both have an "AND" clause requiring the mutator's recompute to complete "before the component's separate, unchanged deletion call (`onDeleteClicked()` / `removeAssemblyLine()`) runs." No task in Group 1 (test stubs) or Group 3 (component conversion) mentions `onDeleteClicked` or `removeAssemblyLine` at all — grep confirms zero occurrences in tasks.md. Tasks 3.2/3.3 describe replacing only the reject-mutation statement ("replace `applyRejectSilent(...)` with `factory.rejectLine(productionLine)`"), implicitly relying on the following deletion-call statement staying untouched, but nothing states this as an explicit constraint or adds a test asserting the ordering. A careless single-function rewrite during 3.2/3.3 could reorder or drop the deletion call with no task or stub catching it.
[Task 1.3 — "both branches" coverage vs R5.S9's two-mutator surface] — R5.S9 covers two mutators (`rejectLine(pl)`, `rejectAssembly(recipe)`) each with two `rejectPrompt` branches (`"always"` vs. other). Task 1.3 says to cover "both the `rejectPrompt === "always"`... and other... branches" but doesn't explicitly say this branch coverage must be exercised for *both* `rejectLine` and `rejectAssembly` (four cases total: line×always, line×other, assembly×always, assembly×other) rather than just one mutator with both branches and the other mutator with one. Ambiguous whether 2×2 coverage is intended or assumed loosely satisfied by "cover both... branches" read across the whole task.
[Task 1.4 — four `choice` values named, no per-mutator cross product called out] — Similarly, task 1.4 lists all four `choice` values (`"never"`, `"no"`, `"yes"`, `"always"`) but doesn't state whether each of `rejectLineChoice`/`rejectAssemblyChoice` needs all four exercised, or whether the four choices only need to be covered once across both mutators combined (e.g., `"never"` tested only via `rejectLineChoice`, `"always"` only via `rejectAssemblyChoice`). `applyRejectChoice`'s branches (`"never"` sets `rejectPrompt`; `"always"` sets `rejectPrompt` AND denies; `"yes"` denies only; implicit `"no"` no-op) are mutator-agnostic, so under-specifying this is lower risk than the 1.3 gap, but still leaves the concrete test matrix to the implementer's judgment rather than the task.
[No task cross-references R4's widened requirement text] — R4's requirement prose (not a scenario) states enforcement "covers both suggestions.ts helpers... not only the one the bug happened to use," which is fully realized by task 1.6/2.2/2.3/3.3, so this isn't a gap in outcome — noting only that no task explicitly quotes or ties back to this specific normative sentence; low value to flag further since scenario-level coverage (R4.S4) is what's actually tested.
[Verification tasks — no E2E/lighthouse task] — Confirmed correct, not an oversight. This is a pure model-layer + test change (no markup, no component visual/interaction surface, no drawer/dialog lifecycle, no localStorage persistence, no multi-step user flow) — none of AGENTS.md's E2E triggers apply, and unit tests are the correct tier per its own test-selection table. Group 4's `npm run test:run` / `lint-fix` / `build` / `make verify` sequence is the right and complete verification set for this change's actual surface.
[Scenario coverage — complete] — Confirmed all six new/modified scenarios (R4.S3, R4.S4, R5.S7, R5.S8, R5.S9, R5.S10) map to a Group 1 stub (1.5, 1.6, 1.1, 1.2, 1.3, 1.4 respectively) with no scenario left uncovered.
[Group ordering — correct] — Confirmed Group 1 (all six stubs + the fail-first check in 1.7) fully precedes Group 2 (mutators), which precedes Group 3 (component conversion), which precedes Group 4 (verification). Task 2.4 re-runs only the mutator-behavior stubs (1.1-1.4, which don't depend on component conversion); task 3.5 re-runs the mutation-contract stubs (1.5, 1.6, which do depend on component conversion). This split is correct — 2.4 doesn't try to re-run 1.5/1.6 before Group 3 exists to satisfy them, and 3.5 doesn't redundantly re-run 1.1-1.4. Dependency order is sound throughout.

## Pass 2 — 2026-07-12

**Source: Reviewer**

**Status: APPROVED**

### Resolved from Previous Pass
[Task 2.2/2.3 — import instruction ambiguity] — RESOLVED. New task 2.2 names the exact existing block verbatim (`app/models/factory.ts:22-25`), explicitly instructs "Extend this one existing statement (do not add a second `from "./suggestions"` import)," and lists all three names (`applyRejectSilent`, `applyRejectChoice`, `lineRecipeSlugs`) to add in that single edit. The old 2.2/2.3 split into four single-concern tasks (2.2 import / 2.3 silent mutators / 2.4 choice mutators / 2.5 re-run stubs). No remaining ambiguity about which statement to edit or when.
[R5.S9/R5.S10 ordering clause — no task verified it] — RESOLVED as a deliberate, disclosed trade-off. Tasks 3.2 and 3.3 now each carry an explicit "Ordering constraint" sentence naming the exact statements that must stay unmoved (`props.onDeleteClicked()` / `removeAssemblyLine(recipe)` in 3.2; additionally `setRejectTarget(null)` in 3.3), tying back to "R5.S9's AND clause" by name. No new automated test was added to verify this at runtime — the coordinator's stated rationale ("the ordering was already true and untested pre-change; a task-level implementation guard is the proportionate response for a fix this size") is accepted: this is a single-statement surgical replacement in a function the task text pins down precisely, and the pre-change code had no such test either, so this isn't a regression in rigor. Accepted, not blocking.
[Task 1.3 — 2×2 matrix] — RESOLVED. Task 1.3 now spells out all four cases explicitly: "both `factory.rejectLine(pl)` and `factory.rejectAssembly(recipe)`, each exercised with `rejectPrompt === "always"`... and with `rejectPrompt` at another value... four cases total, not 'cover both branches' satisfied by only one mutator." Unambiguous.
[Task 1.4 — per-mutator choice coverage] — RESOLVED. Task 1.4 now specifies full 4-value coverage via `rejectLineChoice` and explicit partial (2+, including `"always"`) cross-check via `rejectAssemblyChoice`, with stated rationale ("the two mutators share identical wrapper logic over the same helper, so partial cross-check... is sufficient"). Concrete and actionable; no longer left to implementer judgment.
[No task cross-references R4's widened requirement text] — Not re-addressed and not expected to be; Pass 1 noted this was low-value/non-blocking, outcome-level coverage (R4.S4) is what matters and that remains fully covered.
[Verification tasks — no E2E/lighthouse] — Unchanged; still correctly absent, not re-raised.

### Findings
(none — re-verified task 2.2's cited line range and import list against the current `app/models/factory.ts:22-25`, confirmed the renumbered Group 2 sequence (2.1-2.5) has no dangling references to the old numbering, and confirmed 3.2/3.3's ordering-constraint sentences correctly name the actual next-statement calls (`props.onDeleteClicked()`, `removeAssemblyLine(recipe)`, `setRejectTarget(null)`) present in `ProductionLineComponent.tsx`. No new issues introduced by the rewrite.)
