## Why

Both refactor plans (`plans/model-refactor.md`, `plans/component-refactor.md`) are functionally complete except for their final sweep phase. `app/components/` still has 22 files sitting flat outside the feature directories established in Phases 2-4, several carry the dead `Component` suffix, three `var` declarations remain in `app/models/factory.ts`, `AGENTS.md` documents a state pattern and schema version that no longer exist in the code, and the 2026-07-12 review (`plans/refactor-review.md`) left a handful of small vestigial items (dead guards, a stray double import, an unreconciled `as unknown as`, an unused game-data barrel export) unresolved. None of this blocks functionality — it's cleanup debt that accumulated because Phases 1-4 intentionally deferred non-blocking naming/dead-code work to stay scoped. This is the last item in `plans/plan-order.md` Block B; closing it out finishes both plans and unblocks Block C.

## What Changes

- Move all 22 remaining flat files in `app/components/` into their feature directories, dropping the `Component` suffix where present, per the mapping table in `specs/component-structure/spec.md` R1.S4 (derived from `plans/component-refactor.md` §3's target tree plus reasoned extensions for files the tree doesn't name explicitly — `LogisticsSection.tsx`, and the composed-parent files `ProductionLine.tsx`/`AssemblyLine.tsx`/`Recipe.tsx` whose child files already exist). Use `git mv` to preserve history. Update every import site.
- Move `app/components/factory/useFactoryPageFlows.ts` to `app/hooks/` (it's a hook, not a component — separate from the 22-file component move above).
- Rename `tests/unit/models/library-ops.test.ts` → `tests/unit/models/migrations-merge.test.ts` (verified: it tests `mergeLibrary`/`mergeSingleFactory`/`remapImportedLibrary` from `migrations.ts`, not a `library-ops.ts` module. **Not** a plain rename to `migrations.test.ts` — that file already exists, covering a different requirement set, `schemaVersion` pinning and `migrateLibrary`/module-boundary; the two stay separate files).
- Also update non-import references broken by the `Component`-suffix drop that a plain import-site sweep would miss: the stale comment at `app/models/factory.ts:305` ("per AssemblyLineComponent render"), and the two unit tests that hardcode old literal paths — `tests/unit/production-line-structure.test.ts:22,36` and `tests/unit/contexts/prop-contract.test.ts:47,48` (`"app/components/ProductionLineComponent.tsx"` / `"AssemblyLineComponent.tsx"`) — plus rename `tests/integration/AssemblyLineComponent.test.tsx` to match its new source file name.
- Remove the 3 `var` declarations in `app/models/factory.ts` (lines 239, 240, 342) → `let`/`const`.
- Delete dead code flagged by `npm run knip` (25 unused exports + 8 unused types as of this proposal; exact list enumerated in design.md since it may drift slightly before implementation) and flip `knip.json`'s `exports`/`types` rules from `warn` to `error`, closing the severity-flip B0.4 deferred to this change. `files`/`dependencies` stay `error` (already configured, unchanged).
- Remove the unused `powerPart` re-export from `app/models/game-data/index.ts`'s barrel — confirmed via grep it has zero external consumers (the one internal user, `generator-recipes.ts`, imports it directly from `./load`). `partLookup`/`buildingLookup` are **kept** on the barrel despite also showing zero current consumers: the `game-data` spec's R2 ("Exported data unchanged") explicitly pins them as guaranteed public API, a deliberate decision from an earlier change — removing them would be a spec-breaking call this change doesn't need to make. `powerPart` was never part of R2's pinned list, so trimming it is a plain dead-code removal with no spec impact.
- Close the last models-layer `as unknown as` at `app/models/factory-storage.ts:226`. Confirmed load-bearing, not spurious: it lets `normalizeRecipeOptimizer` tolerate a legacy `string[]` shape for `optimizer.availableParts` (pre-`AvailablePart[]` schema) by forcing an already-`RecipeOptimizerConfig`-typed value through `unknown`. The fix types the *raw* input honestly (as `unknown`/a loosely-typed raw shape) instead of casting a falsely-precise type through `unknown` — same runtime normalization (`string` → `{ partSlug, rate: 0 }`, object passes through), zero cast survives. A regression test fixture covering the legacy `string[]` path is required (see `optimizer-config` capability below) since this is a migration-behavior-adjacent fix, not pure cleanup.
- Fix `AGENTS.md`: remove the dead "`Factory.update()` injected by `FactoryComponent`" claim (both are gone post-M4); correct "schemaVersion 2" to the current value (`CURRENT_SCHEMA_VERSION = 1`) and note migration now lives in `app/models/migrations.ts`; update the directory map to match the final `app/components/` tree.
- Resolve the remaining §6.2 vestigial carryovers: dead `!library` guards in `SourceFactoriesEditor` (`:46,:62`), unnecessary `GraphProps` optionality (`LogisticsSection.tsx:41-42`, moving with that file), `factory.ts`'s double factory-recipe import, duplicated reveal-block markup in `ProductionLineDetails`.
- Fold in logging cleanup (`codebase-improvements.md` #14) if any commented-out `console.*` lines remain (check first — M2 may have already removed them).
- Update integration-test imports to match all moved/renamed files.

**Not in scope** (verified, not a real bullet): removing re-exported `clickableClass` string constants — `grep -rn clickableClass app/ tests/` returns zero matches anywhere; already gone (likely Phase 1). Dropped from this proposal to avoid sending the implementer hunting for nonexistent code.

No new capability, no behavior change except the deliberately-scoped `factory-storage.ts` type-honesty fix (behavior-preserving, regression-tested). `npm run lint-fix` runs at the end of the sweep.

## Capabilities

### New Capabilities

(none — this is a structural sweep, not a new capability)

### Modified Capabilities

- `component-structure`: currently scoped only to the library drawer split (Phase 4a, R1-R3). This change generalizes R1's file-placement rule repo-wide (every `app/components/` file lives under its feature directory; no flat files at the `app/components/` root, full mapping table for all 22 files) and adds new requirements for the no-`Component`-suffix convention (R4), hooks-not-in-components placement (R5), and the knip dead-code/severity-flip gate (R6). R2 (CRUD orchestration) and R3 (library selector contract) are untouched — they stay library-specific and remain true. Repo-wide "`ui/` is domain-free" and "frozen selector contract" are already owned by the `ui-primitives` spec (R9, R10) and are not duplicated here.
- `optimizer-config`: adds a requirement that `availableParts` deserialization in `factory-storage.ts` reads the raw legacy shape without an `as unknown as` cast, while preserving the string→`AvailablePart` normalization behavior exactly (regression-tested).
- `recipe-type-model`: the note under R3 currently exempts "the `factory-storage` serialized-parts cast" from the cast-sweep as out-of-scope. That exemption is removed now that `optimizer-config`'s new requirement closes it — the note is trimmed to keep only the still-valid React Flow exemption.

(No change to `game-data` — R2's pinned barrel exports are untouched; the `powerPart` trim has no spec impact.)

## Impact

- **Code**: 22 file moves/renames in `app/components/` (full table in spec), 1 hook file move (`useFactoryPageFlows.ts` → `app/hooks/`), import updates across all consumers of moved files, one stale comment fix (`factory.ts:305`), `app/models/factory.ts` (var removal, double-import fix), `app/models/factory-storage.ts` (last `as unknown as`, behavior-preserving), `app/models/game-data/index.ts` (barrel trim), `SourceFactoriesEditor`, `GraphProps` type, `ProductionLineDetails`, `knip.json` (severity flip) plus deletion of ~33 knip-flagged unused exports/types.
- **Tests**: import paths update across `tests/unit/`, `tests/integration/`; `library-ops.test.ts` → `migrations-merge.test.ts` rename (distinct from the pre-existing `migrations.test.ts`); two structural unit tests with hardcoded old paths updated (`production-line-structure.test.ts`, `contexts/prop-contract.test.ts`); `AssemblyLineComponent.test.tsx` renamed; one new regression test for the `factory-storage.ts` legacy `string[]` `availableParts` path; no other test behavior changes (aria-labels/`data-testid`s frozen per `component-refactor.md` §5 — the explicit safety contract for the whole sweep).
- **Docs**: `AGENTS.md` (state-pattern section, schemaVersion note, directory map).
- **No runtime/behavior change** outside the one documented, regression-tested `factory-storage.ts` fix — e2e suite is the regression safety net; `make verify` plus `npm run knip` (error severity) are the merge gates.
