## Pass 1 — 2026-07-11

**Source: Reviewer**

**Status: CONCERNS**

### Resolved from Previous Pass


### Findings
[R1.S1 — no test/verification task] — No task anywhere in `tasks.md` verifies file responsibilities (e.g. `LibraryTree.tsx` contains only root-level sort + top-level list, not re-entered for nested levels; `LibraryFactoryMenu.tsx` contains only the `<Menu>`). The extraction tasks (3.1, 4.1, 5.1, 6.1, 7.1) create the files per design.md's contracts but no task cites R1.S1 or includes a check that each file's content boundary matches the requirement text. Since this isn't naturally unit-testable (it's a "what's in this file" structural property, not a runtime behavior), it needs an explicit verification task (e.g. a code-review checklist item in Group 9, or folding the check into each extraction task's own acceptance note) — currently absent.
[R1.S3 — no test/verification task] — No task verifies that all six transient state vars stay owned by `LibraryDrawer.tsx` and no row component introduces a competing copy. Task 7.2 implements this (states `LibraryDrawer.tsx` "owns `rowState`'s six `useState` calls") but no task in Group 9 (Verification) or elsewhere checks it post-hoc, and no unit/integration test stub in Group 1 targets it. Like R1.S1, this is a structural property rather than a runtime behavior, so it warrants an explicit verification task, not a unit test stub — currently neither exists.
[R2.S2 — no test/verification task] — No task checks that `app/components/library/*.tsx` don't import `saveLibrary`/`addFactory`/`updateFactory`/`removeFactory`/`addFolder`/`renameFolder`/`removeFolder`/`moveFactory` from `app/models/storage-service`. This is directly analogous to R3.S2's grep-based selector-contract check (task 9.4) and R1.S2's grep-based file-removal check (task 8.3) — both of which got explicit grep verification tasks — but R2.S2 has no equivalent (e.g. `grep -rn "from \"@/app/models/storage-service\"\|from \"../../models/storage-service\"" app/components/library/` and confirm none of the forbidden names appear). Easy to add given the precedent already set by 8.3/9.4; currently missing.
[Group 1 stub coverage vs. D1b's 9 `rowActions` members] — Group 1's stubs (1.1–1.8) only cover the 7 mutator functions surfaced on `useLibrary` (R2.S1/R2.S3). They don't cover `rowActions` members that aren't mutators — `toggleFolder`, `commitRename`, `closeMenu`, `handleAddFolder` (the 4 non-setter `rowActions` members per D1b's table) have no unit test stubs anywhere, even though they're as testable in isolation as the mutators (e.g. "`toggleFolder` flips membership in `expandedFolders`," "`commitRename` calls `renameFactory` when `editState.type === 'factory'`"). Not flagged as blocking since these are component-internal (not hook-surface) and might reasonably be covered by the existing E2E suite's interaction flows instead, but tasks.md doesn't state that as the rationale — it's silent on why Group 1 stops at the mutator surface.
[Task 7.2 — single task carries excessive D1b-table responsibility] — Task 7.2 is the only task implementing `LibraryDrawer.tsx`, and per its own description must: own all six `useState` calls, implement `handleAddFolder`/`handleExportAll`, own the `deleteConfirmFolder` dialog, read `library` via context, build `rowState` via `useMemo`, build `rowActions` via `useCallback` for all 9 members with correct per-member deps (per D1b's table — `toggleFolder` empty deps, `commitRename` deps `[editState, libraryApi.renameFactory, libraryApi.renameFolder]`, `closeMenu` empty deps, `handleAddFolder` deps `[libraryApi.addFolder]`), render the `Drawer`/header/footer chrome, and wire `LibraryFactoryMenu`'s six props. This is the single largest, most error-prone task in the whole plan (matches this document's own design-review history, which needed 10 passes to nail down exactly this table) but is one unchecked task item with no sub-task breakdown and no dedicated verification step (e.g. "confirm `rowActions`' 9 members and their deps arrays match D1b's table row-by-row") beyond the generic "app still builds" check in 7.3. Given how much D1b's table itself churned across design-review passes, a single generic build-check is weak insurance against a subtly wrong deps array (e.g. omitting `libraryApi.addFolder` from `handleAddFolder`'s deps, or including `library` in `commitRename`'s deps when it should be `libraryApi.renameFactory`/`renameFolder`) — recommend splitting 7.2 into per-member or per-concern sub-tasks, or adding an explicit row-by-row cross-check against D1b's table as its own task.

## Pass 2 — 2026-07-11

**Source: Reviewer**

**Status: CONCERNS**

### Resolved from Previous Pass
[R2.S2 — no test/verification task] — New task 1.9 greps `app/components/library/` for the 8 forbidden `storage-service` function names, matching R2.S2's assertion exactly (same name list, same target directory). Resolved.
[Group 1 stub coverage vs. D1b's 9 `rowActions` members] — New note (after 1.11) explicitly states why `toggleFolder`/`commitRename`/`closeMenu`/`handleAddFolder` aren't unit-stubbed: they're UI interaction behaviors covered by the existing E2E/integration suite (R3.S1, task 9.2), citing AGENTS.md's test-type guidance (component/interaction behavior → integration/E2E, not unit). This directly matches AGENTS.md's own stated table ("Integration... component behavior, user interactions"; "E2E... multi-step flows"). Resolved — the rationale is now explicit and consistent with the project's documented conventions.
[Task 7.2 — single task carries excessive D1b-table responsibility] — Task 7.2 split into 7.2 (implementation) + new 7.3 (explicit row-by-row cross-check naming all 9 `rowActions` members and their exact `useCallback` deps arrays, matching D1b's table verbatim) + renumbered 7.4 (build check). Verified 7.3's deps arrays (`toggleFolder: []`, `commitRename: [editState, libraryApi.renameFactory, libraryApi.renameFolder]`, `closeMenu: []`, `handleAddFolder: [libraryApi.addFolder]`) match design.md's D1b table exactly. Also checked for orphaned references to the old numbering elsewhere in the document (e.g. task 9.2 still correctly cites itself, no stale "7.2"/"7.3" cross-references found) — renumbering is clean. Resolved.
[R1.S3 — no test/verification task] — New task 1.11 greps for `useState` across all `library/*.tsx` files and confirms the 6 named state values appear only in `LibraryDrawer.tsx`. This is a faithful proxy for R1.S3's "no row component introduces its own competing copy of this state" assertion — a stray `useState` for e.g. `editState` in `LibraryFactoryRow.tsx` would surface in the grep and fail the check. Resolved.

### Findings
[R1.S1 — new task 1.10 verifies file *existence*, not R1.S1's actual content-boundary assertion] — Task 1.10 (`ls app/components/library/` lists exactly the 6 filenames) confirms the file *set* exists with the right names — but R1.S1's scenario text is specifically about **what each file contains** ("`LibraryDrawer.tsx` contains only the `Drawer`/pinned-sidebar chrome... `LibraryTree.tsx` contains only root-level sorting... `LibraryTree.tsx` is not re-entered for nested levels..."), not whether the files exist. A file named `LibraryTree.tsx` that happens to also contain folder-recursion logic (violating "not re-entered for nested levels") would still pass task 1.10's `ls` check. This is also largely redundant with task 8.3 (which already confirms, from the opposite direction, that `FactoryLibraryDrawer.tsx` — the one-big-file alternative — is gone and unimported) and with R1's parent requirement text (file existence), rather than closing the gap on R1.S1's scenario specifically (content boundaries per file). A closer fix would be a manual/reviewer checklist task cross-referencing each file's actual top-level JSX/import list against R1.S1's per-file description (similar in spirit to how 7.3 now does a row-by-row cross-check against D1b's table) — e.g. "confirm `LibraryTree.tsx` contains no `Menu`/`ConfirmDialog`/`Drawer` JSX and no self-import" per file. Lower severity than Pass 1's original finding (something now exists, and it's not wrong, just incomplete against the specific scenario it's cited for) — recommend either strengthening 1.10 into a content check or explicitly noting file-content-boundary review happens as part of `review.md`'s eventual diff pass (making that reliance explicit rather than implied).

## Pass 3 — 2026-07-11

**Source: Reviewer**

**Status: CONCERNS**

### Resolved from Previous Pass
[R1.S1 — task 1.10 verifies existence only, not content boundaries] — Task 1.10 now has an added sentence: "This check alone doesn't verify per-file content boundaries... that's a manual review checklist, not a grep: see task 9.7." New task 9.7 is a manual content-boundary review checklist naming, per file, what R1.S1 says it should contain: `LibraryDrawer.tsx` (chrome/header/footer, no per-row logic), `LibraryTree.tsx` (root-level sort + one non-recursive pass, no self-recursion, no folder-child lookup logic), `LibraryFolderRow.tsx` (the only self-recursing file), `LibraryFactoryMenu.tsx` (only the `<Menu>` + its `ConfirmDialog`), `MoveToFolderSelect.tsx` (only the `<TextField select>` block). This closes the "grep can't verify content boundaries" gap with an explicit, appropriately-scoped manual task rather than overreaching a static check past what it can actually verify — the right shape of fix. Partially resolved — see new finding below for a coverage gap in 9.7 itself.

### Findings
[Task 9.7 omits `LibraryFactoryRow.tsx`] — R1.S1's scenario text describes content boundaries for all six split files, including: "`LibraryFactoryRow.tsx` renders exactly one factory row (load, rename, actions-menu trigger) and composes `MoveToFolderSelect` when moving." Task 9.7's checklist covers `LibraryDrawer.tsx`, `LibraryTree.tsx`, `LibraryFolderRow.tsx`, `LibraryFactoryMenu.tsx`, and `MoveToFolderSelect.tsx` — five of the six files — but never mentions `LibraryFactoryRow.tsx`. Confirmed via direct read of task 9.7's text and cross-check against R1.S1: the omission is real, not a paraphrase. Given `LibraryFactoryRow.tsx` is arguably the most content-sensitive file of the five *not* covered by a grep-based check (it's the only row file besides `LibraryFolderRow.tsx` that isn't caught by 1.11's `useState`-location check for its own boundary violations, and it's the file most likely to accidentally absorb `LibraryFactoryMenu`'s menu-rendering or `MoveToFolderSelect`'s dropdown JSX inline instead of composing them, since it's adjacent to both), leaving it out of the one checklist meant to catch exactly this class of boundary violation is a real gap, not a nitpick. Fix: add a fifth clause to 9.7, e.g. "`LibraryFactoryRow.tsx` renders exactly one factory row (load, rename, actions-menu trigger) and composes `MoveToFolderSelect` inline when moving, without inlining `MoveToFolderSelect`'s or `LibraryFactoryMenu`'s own JSX."

## Pass 4 — 2026-07-11

**Source: Reviewer**

**Status: APPROVED**

Full-document cold read this pass.

### Resolved from Previous Pass
[Task 9.7 omits `LibraryFactoryRow.tsx`] — Task 9.7 now reads (verified verbatim): "...`LibraryFactoryRow.tsx` renders exactly one factory row (load/rename/actions-trigger) and only *composes* `MoveToFolderSelect` (doesn't inline its `<TextField select>` JSX) and only *triggers* `LibraryFactoryMenu` (doesn't inline the `<Menu>`/`ConfirmDialog` JSX)..." — all six split files are now covered by the checklist, with the fix going further than the minimum suggested (also explicitly names what `LibraryFactoryRow.tsx` must *not* inline from its two composed children, not just what it does contain). Resolved.

### Findings
None. Re-verified full spec-scenario coverage (R1.S1 → 1.10 + 9.7; R1.S2 → 8.3; R1.S3 → 1.11; R2.S1 → 1.1–1.7; R2.S2 → 1.9; R2.S3 → 1.8; R3.S1 → 9.2; R3.S2 → 9.4 — all 9 scenarios now have a task), Group 1 fully precedes implementation, and dependency order matches D6 exactly (mutators → MoveToFolderSelect → LibraryFactoryMenu → LibraryFactoryRow → LibraryFolderRow → LibraryTree+LibraryDrawer → swap+delete → verification). Spot-checked additional details not previously scrutinized this closely: `LibraryTree.tsx`'s 9.7 clause ("root-level sort + one non-recursive render pass, no self-recursion, no folder-child lookup logic") adequately captures R1.S1's "not re-entered for nested levels" even though it doesn't restate "renders LibraryFolderRow/LibraryFactoryRow" verbatim — not a gap, the recursion boundary is the property under test. `LibraryFactoryMenu`'s "rendered exactly once, not per-row" property (from design.md, not literally in R1.S1's text) is enforced by construction via `LibraryFactoryRow`'s "only triggers, doesn't inline" clause — no dedicated single-instance check needed since no row can render its own copy. No further contradictions, stale cross-references, or missing task coverage found.
